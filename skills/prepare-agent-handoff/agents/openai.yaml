interface:
  display_name: "Prepare Agent Handoff"
  short_description: "Build minimal evidence-backed handoffs"
  default_prompt: "Use $prepare-agent-handoff to package only the context a destination lacks into a runnable handoff."
