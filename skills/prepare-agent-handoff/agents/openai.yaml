interface:
  display_name: "Prepare Agent Handoff"
  short_description: "Build evidence-backed handoff contracts"
  default_prompt: "Use $prepare-agent-handoff to turn the current context into a self-contained, evidence-backed handoff contract."
